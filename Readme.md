# ChatGPT cli

Experiment using the OpenAI's API to build a ChatGPT in the terminal app.

## Setup

Create a `.env` file:

```
OPENAI_API_KEY="sk-***"
```

and then run:

```
yarn install
./src/index.tsx chat
```
